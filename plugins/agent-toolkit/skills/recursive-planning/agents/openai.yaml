interface:
  display_name: "Recursive Planning"
  short_description: "Plan, execute, and verify a bounded task"
  default_prompt: "Use $recursive-planning to define acceptance criteria, execute the necessary work, and verify the result without expanding into unrelated improvements."
